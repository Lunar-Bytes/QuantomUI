from setuptools import setup, find_packages

setup(
    name="Quantom",
    version="0.1.0",
    description="A modern Python GUI library built on Tkinter",
    author="Lunar",
    packages=find_packages(),
    python_requires=">=3.7",
    install_requires=[],
)
